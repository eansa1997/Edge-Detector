// compile commands
javac main.java

// run commands with arguments, data.txt must be a txt file with an image header and pixel data.

java main data.txt robertOut.txt sobelOut.txt gradientOut.txt prettyOut.txt debugOut.txt

// you can then run an automatic threshold value selector and perform binary thresholding to see final results.